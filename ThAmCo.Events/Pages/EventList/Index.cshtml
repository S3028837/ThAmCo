@page
@model ThAmCo.Events.Pages.EventList.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].ReservationReference)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].FoodBookingId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].EventName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].EventType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].EventDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Event[0].GuestBookings)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Event) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ReservationReference)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodBookingId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventType)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EventDate)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.GuestBookings.Count)
                </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.EventId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.EventId">Details</a> |
                    <a asp-page="./Delete" asp-route-id="@item.EventId">Delete</a> |
                    <a asp-page="./Guests" asp-route-id="@item.EventId">Guests</a> |
                    <a asp-page="./EventStaffFunctions/EventStaff" asp-route-id="@item.EventId">Staff</a>
            </td>
        </tr>
}
    </tbody>
</table>
